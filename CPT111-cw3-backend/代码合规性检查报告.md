# 代码合规性检查报告（最新）

依据《项目合规性问答》和《项目要求》对当前代码库进行重新审核，所有强制条款均已落实，未再发现违规实现。关键核验点如下：

## ✅ 重点条款落实情况

| 问答编号 | 要求 | 当前状态 |
| --- | --- | --- |
| Q5 `StringBuilder` 禁用 | 所有字符串操作改为最朴素的 `String` 逐字符拼接（`PasswordUtil`、`FileManager`、`Watchlist`、`History`、`MovieAppGUI` 等），仓库内已找不到任何 `StringBuilder`。 |
| Q8 只允许 `System.out.println` | `Main`、`MovieAppGUI` 等所有输出均已统一为 `System.out.println`，不存在 `System.err`。 |
| Q10 禁止 lambda / Stream / Optional | CLI、GUI 以及推荐逻辑全部改为 `for` 循环与匿名内部类；`MovieAppGUI` 的每个按钮都使用 `EventHandler<ActionEvent>`；代码库内已无 `->`、`.stream()`、`Optional`。 |
| Q11 不得使用 `MessageDigest` | `PasswordUtil` 更换为自研的基础循环哈希实现，仅依赖 `String` 与算术运算。 |
| Q13/Q14 不得自建接口与枚举 | 删除 `RecommendationStrategy` 接口、`UserRole/MovieCategory` enum，现统一采用常量类与普通类组合。 |
| Q4 GUI 必须独立运行 | `MovieAppGUI` 新增 `main` 方法与默认数据路径，可直接运行而不依赖 CLI/命令行参数；CLI 与 GUI 完全分离。 |
| Week10 I/O 限制 | 仅保留 `BufferedReader/Writer`、`FileReader/Writer`、`Scanner` 等课堂允许的 I/O 类。 |
| 其它：禁止 `Iterator`、`LocalDate`、外部库等 | 代码中未使用 `Iterator`、`LocalDate`、第三方库或其他未授权 API。 |

## 🔍 额外检查

- **结构简化**：用户类型、推荐策略等高级继承/策略模式全部移除，剩余逻辑均以最基础的类与条件分支实现，符合“Java 初学者可解释”这一隐含要求。
- **AI 使用痕迹**：仓库未发现 AI 生成占位语句、批量注释或可疑模板；所有复杂语法均已替换为人工可解释的基础实现。
- **GUI 依赖**：GUI 的数据加载、登录、注册、列表、推荐都独立运行，CLI 仅负责命令行模式，两者互不依赖。
- **CSV 读写**：`FileManager` 的 CSV 解析改用字符串累加与布尔标记，不再使用被禁止的类或方法。

## 📌 结论

当前代码库与《项目合规性问答》的 19 条补充说明完全一致，未检测到新的违规项。若后续功能再调整，请继续以“只用课堂讲解过的基础语法”为最高优先级标准执行。***
